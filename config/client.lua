return {
    -- Keybind that opens the radio, and the label shown in the FiveM key settings
    openKey = 'Q',
    openKeyLabel = 'Open Radio',

    -- Key held to transmit while voice broadcasting
    pttKey = 'LMENU',
    pttKeyLabel = 'ALT',

    -- Turn the radio off automatically when the player leaves the vehicle
    stopOnExitVehicle = true,

    -- Draw broadcast zone outlines in-world, useful while placing zones
    zoneDebug = false,

    -- Distance the station editor raycast reaches when picking coordinates
    raycastDistance = 25.0,

    -- Command that opens the station editor for authorised players
    editorCommand = 'radiostations',
}
