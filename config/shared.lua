return {
    -- Print verbose module logs to the console
    debug = false,

    -- Volume slider granularity (0, 10, 20, ... 100)
    volumeStep = 10,

    -- Volume assigned to a listener that has not set one yet (0-100)
    defaultVolume = 50,
}
